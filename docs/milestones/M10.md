# M10 — Anexos de requisições e tarefas

## Objetivo
Permitir anexos de arquivos e links em requisições e tarefas.

## Dependências
- M01/M02.
- M08/M09.
- Não depende do storage de releases.

## Tarefas
1. Criar tabela `attachments`.
2. Criar tabela `attachment_blobs`.
3. Implementar `AddFileAttachment`.
4. Implementar `AddLinkAttachment`.
5. Implementar `ListAttachments`.
6. Implementar `GetFileAttachment`.
7. Implementar `RemoveAttachment`.
8. Validar tipo, tamanho e URL.
9. Criar endpoints de upload, listagem, download e remoção.
10. Garantir transação entre metadados e blob.
11. Validar permissões e isolamento tenant.

## Critérios de aceitação
- Imagens e PDFs são gravados em `attachment_blobs`.
- Links externos são salvos como metadados.
- Download retorna arquivo íntegro.
- Tipos inválidos e arquivos acima do limite são rejeitados.
- Listagem não carrega o `bytea`.
- Remoção é atômica.
- Cascade funciona ao excluir requisição ou tarefa.
- Testes de API, transação e isolamento passam.

## Decisões existentes
- PostgreSQL é a única fonte de verdade dos anexos.
- Arquivos usam `BYTEA` em tabela dedicada.
- Limite inicial recomendado: 10 MB por arquivo.
- Storage externo é rejeitado para anexos.
- Large Objects ficam reservados para evolução futura.
- Anexo não altera status nem capacidade.

## Status
**Concluída.** Domínio, validações iniciais, persistência base, operações de anexos e integração HTTP concluídos.

### Concluído nesta etapa
- Entidade imutável `Attachment` para `FILE` e `LINK`.
- Owner discriminado de `Requisition` ou `Task`.
- Invariantes estruturais de FILE/LINK e normalização de metadata.
- Validação de filename, URL, tamanho e MIME.
- Detecção de MIME por magic bytes para os cinco formatos oficiais.
- Cálculo de checksum SHA-256 sobre o conteúdo real.
- Testes de domínio e validação pura.
- Contratos de repositories de metadata/blob e Unit of Work.
- Repositories Drizzle tenant-aware para metadata e BYTEA.
- Unit of Work Drizzle compartilhando a mesma transação.
- Testes condicionais de persistência PostgreSQL, rollback e cascades.
- `AddFileAttachment` com validação de conteúdo e transação metadata/blob.
- `AddLinkAttachment` com persistência direta de metadata.
- Testes unitários de autorização, isolamento, validação e rollback.
- `ListAttachments` sem carregamento de blobs.
- `GetFileAttachment` com validação de blob e checksum SHA-256.
- `RemoveAttachment` usando cascade de blob do banco.
- Testes unitários de listagem, download, integridade e remoção.
- Plugin multipart, composição root e `TestModules` integrados.
- Rotas HTTP de Requisition e Task com OpenAPI, upload, download e remoção.
- Testes HTTP básicos de multipart, headers, listagem e remoção.
- Correções finais de contrato multipart, field name `file`, OpenAPI de upload/owner e integridade de download.
- Testes HTTP de estrutura multipart, limite 413 e inconsistência de `sizeBytes`.
- PostgreSQL real: 13 passed, 0 skipped.

### Encerramento
- Correções finais: somente `fieldname === "file"`, arquivo obrigatório, title único e rejeição de partes inválidas.
- Violações estruturais multipart retornam 400; arquivo acima de 10 MB retorna 413.
- Upload FILE é documentado como multipart com `file` binário obrigatório, `title` opcional e `additionalProperties: false`.
- Owner é documentado como união discriminada entre Requisition e Task.
- Inconsistência entre `sizeBytes` e o Buffer do download retorna 422 via `BusinessRuleError`.
- Testes HTTP: **7 passed, 0 skipped**.
- Suíte M10 sem banco: **77 passed, 13 skipped**; skips exclusivamente PostgreSQL condicional.
- PostgreSQL real: **13 passed, 0 skipped** com `TEST_DATABASE_URL` em `localhost:5433/orbis_test`.
- Lint e `git diff --check` aprovados.
- Typecheck bloqueado exclusivamente pelo erro preexistente de `local-artifact-storage`, fora do escopo.
- Não há pendências bloqueantes. Melhoria opcional: teste PostgreSQL explícito de checksum e `sizeBytes` após restauração.

> Milestone grande: envolve persistência, validação, transações, segurança, endpoints e integração de UI.
