# M09 — Tarefas e histórico de status

## Objetivo
Implementar a entidade `Task`, suas transições e o histórico imutável.

## Dependências
- M04–M06.
- M08 opcionalmente.

## Tarefas
1. Criar entidade `Task`.
2. Implementar estados `TODO`, `IN_PROGRESS`, `PAUSED`, `DONE`.
3. Implementar transições de status.
4. Criar `TaskStatusHistory`.
5. Implementar criação, atualização, listagem, consulta e transição.
6. Aplicar permissões `tasks.*`.

## Critérios de aceitação
- Toda mudança de status gera histórico.
- Transições inválidas retornam `BusinessRuleError`.
- Histórico não é editável.
- Permissões são aplicadas.
- Testes cobrem todas as transições.

## Decisões existentes
- Não criar estados adicionais sem necessidade documentada.
- Transições documentadas incluem `TODO → IN_PROGRESS`, `IN_PROGRESS → PAUSED`, `PAUSED → IN_PROGRESS` e `IN_PROGRESS → DONE`.
- Regra `PAUSED → DONE`: **A confirmar**.

## Status
Domínio inicial e infraestrutura de persistência concluídos. Use cases, endpoints e integração permanecem não iniciados.

## Progresso do domínio inicial
- [x] Entidade `Task` com campos, defaults, invariantes e mutações específicas.
- [x] Estados e matriz de transições válidas centralizados na entidade.
- [x] Entidade imutável `TaskStatusHistory` com evento inicial e transições válidas.
- [x] Testes unitários do domínio para criação, restore, mutações, transições e histórico.

## Progresso da infraestrutura de persistência
- [x] Portas `TaskRepository`, `TaskStatusHistoryRepository` e `TaskUnitOfWork`.
- [x] Repositories Drizzle com isolamento tenant, mapeamento de domínio e lock `FOR UPDATE`.
- [x] Unit of Work Drizzle compartilhando repositories na mesma transação.
- [x] Testes PostgreSQL condicionais para persistência, rollback, isolamento, ordenação e concorrência.

## Progresso do primeiro use case
- [x] `CreateTask` valida autorização, memberships e vínculo opcional com Requisition.
- [x] `CreateTask` persiste Task e histórico inicial `null → TODO` na mesma Unit of Work.
- [x] Testes de criação, validação, DTO de saída e rollback transacional.

## Progresso de atualização
- [x] `UpdateTask` implementa PATCH strict com mutações explícitas e referências tenant-aware.
- [x] `UpdateTask` preserva campos ausentes, remove opcionais com `null` e não altera status/completedAt.
- [x] Testes de atualização, autorização, isolamento, referências, estado `DONE` e DTO de saída.

## Progresso de transição de status
- [x] `TransitionTaskStatus` usa `TaskUnitOfWork` e `findByIdForUpdate`.
- [x] Transições válidas persistem Task e histórico na mesma transação.
- [x] Timestamps compartilhados, rollback, concorrência e transições inválidas estão cobertos por testes.

## Progresso de leitura
- [x] `ListTasks` aplica somente os filtros oficiais e respeita o isolamento tenant.
- [x] `GetTask` retorna Task com histórico de status ordenado e serializado.
- [x] Testes de listagem, detalhe, autorização, tenant e histórico concluídos.

## Progresso de integração
- [x] Tasks integradas ao composition root e `OrbisModules`.
- [x] Rotas HTTP protegidas para criação, leitura, atualização e transição de status.
- [x] Schemas OpenAPI, TestModules e testes HTTP concluídos.

## Encerramento da milestone
- [x] Domínio, persistência, use cases, composition root, HTTP e TestModules concluídos.
